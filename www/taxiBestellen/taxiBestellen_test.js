'use strict';

describe('taxiApp.taxiBestellen module', function() {

  beforeEach(module('taxiApp.taxiBestellen'));

  describe('taxiBestellen controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var taxiBestellenCtrl = $controller('taxiBestellenCtrl');
      expect(taxiBestellenCtrl).toBeDefined();
    }));

  });
});